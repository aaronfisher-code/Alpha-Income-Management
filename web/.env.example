# Same-origin API is the production default.
VITE_API_BASE_URL=/api

# Local prototype only. Never define this for a production build.
# VITE_API_TOKEN=

# Enables deterministic representative data when the API is unavailable.
VITE_USE_MOCKS=true
